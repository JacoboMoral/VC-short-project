function [outputData] = generateNoEyes(inputData,ratio)
%0 <= ratio <= 1
% inputData is composed solely of eyes images
    
    newImagesPerEyesImage = round(1/ratio);
    for i = 1:length(inputData)
        for j = 1:newImagesPerEyesImage)
            
        end
    end

    outputData = 5
end

