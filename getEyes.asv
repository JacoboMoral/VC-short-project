function eyes = getEyes(data)
    %for i = 1:length(data.imatges)
    for i = 1:5  
       MAX
        rect = [data.eyes{i}.RX-15 data.eyes{i}.RY-5 (data.eyes{i}.LX-data.eyes{i}.RX) (data.eyes{i}.LY-data.eyes{i}.RY)];
        eye = imcrop(data.imatges{i},rect);
        figure; imshow(eye);

        eyes{i} = eye;
     end
end

